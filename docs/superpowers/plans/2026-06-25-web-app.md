# Web App (HTTP API + Auth + UI) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** A usable web app over the existing engine: register/login, link Riot accounts, see open betting events with odds, place bets, view balance + history.

**Architecture:** Express server (`server/app.js`) exposing a REST API over the existing never-throw stores. Sessions via an HMAC-signed cookie (`server/session.js`, no session library). A single static `index.html` (vanilla JS) is the UI. The `players` balance (auto-created at 1000) and the `users` account share the same `username` key — no reconciliation needed.

**Tech Stack:** Node 20+ (CommonJS), `express`, `pg`, jest + `supertest`.

## Global Constraints

- Node `>=20.0.0`, CommonJS.
- New deps allowed THIS slice: `express` (web framework), `supertest` (devDep, API tests). Session signing uses built-in `node:crypto` — no session library.
- Auth = HMAC-signed cookie named `session`; `SESSION_SECRET` env (dev default allowed).
- The API consumes existing modules unchanged: `accounts`, `store`, `events`, `bets.makeSingle`, `markets`/`riot.getPuuid`.
- Never-throw stores mean the API stays up even with no DB (returns fallback shapes). Integer Clout.
- Bet placement reads the event's stored `board` (JSONB) to freeze odds.

---

### Task 1: Session cookies (`session.js`)

**Files:**
- Create: `server/session.js`
- Test: `server/tests/session.test.js`

**Interfaces:**
- Produces:
  - `sign(username: string) -> string` (cookie value `b64url(username).hmac`)
  - `read(value: string) -> string|null` (username if signature valid, else null)

- [ ] **Step 1: Write the failing tests**

```js
// server/tests/session.test.js
const { sign, read } = require('../session');

test('sign then read round-trips the username', () => {
  const v = sign('GraveDigger');
  expect(read(v)).toBe('GraveDigger');
});

test('read rejects a tampered signature', () => {
  const v = sign('alice');
  const tampered = v.slice(0, -1) + (v.endsWith('a') ? 'b' : 'a');
  expect(read(tampered)).toBe(null);
});

test('read returns null on malformed input', () => {
  expect(read('garbage')).toBe(null);
  expect(read('')).toBe(null);
  expect(read(undefined)).toBe(null);
});
```

- [ ] **Step 2: Run to verify failure** — `npx jest server/tests/session.test.js`. Expected: FAIL.

- [ ] **Step 3: Implement `server/session.js`**

```js
// server/session.js
// Stateless session: an HMAC-signed cookie value "b64url(username).hexsig".
const { createHmac, timingSafeEqual } = require('crypto');

const SECRET = process.env.SESSION_SECRET || 'lolbet-dev-secret-change-me';

function hmac(username) {
  return createHmac('sha256', SECRET).update(username).digest('hex');
}

function sign(username) {
  return `${Buffer.from(String(username)).toString('base64url')}.${hmac(String(username))}`;
}

function read(value) {
  try {
    const [b64, sig] = String(value).split('.');
    if (!b64 || !sig) return null;
    const username = Buffer.from(b64, 'base64url').toString('utf8');
    const expected = hmac(username);
    const a = Buffer.from(sig);
    const b = Buffer.from(expected);
    if (a.length !== b.length || !timingSafeEqual(a, b)) return null;
    return username;
  } catch {
    return null;
  }
}

module.exports = { sign, read };
```

- [ ] **Step 4: Run to verify pass** — `npx jest server/tests/session.test.js`. Expected: PASS (3).

- [ ] **Step 5: Commit**

```bash
git add server/session.js server/tests/session.test.js
git commit -m "feat: HMAC-signed session cookies (no session lib)"
```

---

### Task 2: `store.listBets`

**Files:**
- Modify: `server/store.js` (add `listBets`)
- Modify: `server/tests/store.test.js` (assert disabled fallback)

**Interfaces:**
- Produces: `listBets(username) -> Array<{match_id, bet, stake, status, payout, created_at}>` ([] on error).

- [ ] **Step 1: Add an assertion to `server/tests/store.test.js`** inside the existing disabled-DB test:

```js
    await expect(store.listBets('gd')).resolves.toEqual([]);
```

- [ ] **Step 2: Add `listBets` to `server/store.js`** (before `module.exports`, and add it to the exports):

```js
async function listBets(username) {
  try {
    const res = await db.query(
      `SELECT match_id, bet, stake, status, payout, created_at
       FROM bets WHERE username = $1 ORDER BY created_at DESC LIMIT 50`,
      [username]
    );
    return res.rows;
  } catch (err) {
    if (!isDisabled(err)) console.error('[store] listBets:', err.message);
    return [];
  }
}
```

Add `listBets` to the `module.exports` object.

- [ ] **Step 3: Run** — `npx jest server/tests/store.test.js && npm test`. Expected: green.

- [ ] **Step 4: Commit**

```bash
git add server/store.js server/tests/store.test.js
git commit -m "feat: store.listBets (user bet history)"
```

---

### Task 3: Express API (`app.js`)

**Files:**
- Modify: `package.json` (add `express` dep, `supertest` devDep)
- Create: `server/app.js`
- Test: `server/tests/app.test.js`

**Interfaces:**
- Consumes: `accounts`, `store`, `events`, `session`; `makeSingle` from `./bets`; `getPuuid` from `../src/riot`.
- Produces: `module.exports = app` (an Express app, not yet listening). Routes:
  - `POST /api/register {username,password}` ; `POST /api/login {username,password}` (sets `session` cookie) ; `POST /api/logout`
  - `GET /api/me` → `{username, balance}` (auth) ; `GET /api/accounts` (auth) ; `POST /api/link {riotId}` (auth)
  - `GET /api/events` → open events ; `POST /api/bet {matchId,marketId,side,stake}` (auth) ; `GET /api/bets` (auth)

- [ ] **Step 1: Add deps to package.json** — add to `dependencies`: `"express": "^4.19.2"`; add a `devDependencies` block (or extend it) with `"supertest": "^6.3.4"` (keep `jest`). Then run `npm install`.

- [ ] **Step 2: Write the failing test** `server/tests/app.test.js` (hermetic — no DB, so writes return fallback errors but routes must exist and shapes must be right):

```js
// server/tests/app.test.js
const request = require('supertest');
const app = require('../app');

describe('API (no DATABASE_URL)', () => {
  test('GET /api/events returns an array', async () => {
    const res = await request(app).get('/api/events');
    expect(res.status).toBe(200);
    expect(Array.isArray(res.body)).toBe(true);
  });

  test('GET /api/me without a session is 401', async () => {
    const res = await request(app).get('/api/me');
    expect(res.status).toBe(401);
  });

  test('POST /api/register with no DB returns ok:false', async () => {
    const res = await request(app).post('/api/register').send({ username: 'a', password: 'b' });
    expect(res.body.ok).toBe(false);
  });

  test('POST /api/login with no DB does not authenticate (no cookie, ok:false)', async () => {
    const res = await request(app).post('/api/login').send({ username: 'a', password: 'b' });
    expect(res.body.ok).toBe(false);
  });
});
```

- [ ] **Step 3: Run to verify failure** — `npx jest server/tests/app.test.js`. Expected: FAIL (module missing).

- [ ] **Step 4: Implement `server/app.js`**

```js
// server/app.js
// Express REST API over the never-throw stores. Exports the app (no listen()).

const express = require('express');
const path = require('path');
const accounts = require('./accounts');
const store = require('./store');
const events = require('./events');
const session = require('./session');
const { makeSingle } = require('./bets');
const { getPuuid } = require('../src/riot');

const app = express();
app.use(express.json());
app.use(express.static(path.join(__dirname, 'public')));

// Parse the `session` cookie → req.username (or undefined).
app.use((req, _res, next) => {
  const raw = (req.headers.cookie || '')
    .split(';').map((c) => c.trim()).find((c) => c.startsWith('session='));
  req.username = raw ? session.read(decodeURIComponent(raw.slice('session='.length))) : null;
  next();
});

function requireAuth(req, res, next) {
  if (!req.username) return res.status(401).json({ error: 'auth' });
  next();
}

app.post('/api/register', async (req, res) => {
  const { username, password } = req.body || {};
  res.json(await accounts.register(username, password));
});

app.post('/api/login', async (req, res) => {
  const { username, password } = req.body || {};
  const r = await accounts.authenticate(username, password);
  if (r.ok) res.cookie('session', session.sign(username), { httpOnly: true, sameSite: 'lax' });
  res.json(r);
});

app.post('/api/logout', (req, res) => {
  res.clearCookie('session');
  res.json({ ok: true });
});

app.get('/api/me', requireAuth, async (req, res) => {
  res.json({ username: req.username, balance: await store.getBalance(req.username) });
});

app.get('/api/accounts', requireAuth, async (req, res) => {
  res.json(await accounts.listRiot(req.username));
});

app.post('/api/link', requireAuth, async (req, res) => {
  const { riotId } = req.body || {};
  let puuid;
  try { puuid = await getPuuid(riotId); }
  catch (e) { return res.json({ ok: false, error: 'riot-id' }); }
  res.json(await accounts.linkRiot(req.username, riotId, puuid));
});

app.get('/api/events', async (_req, res) => {
  res.json(await events.listOpen());
});

app.post('/api/bet', requireAuth, async (req, res) => {
  const { matchId, marketId, side, stake } = req.body || {};
  const ev = await events.getEvent(matchId);
  if (!ev) return res.json({ ok: false, error: 'no-event' });
  let bet;
  try { bet = makeSingle({ player: req.username, board: ev.board, marketId, side, stake: Number(stake) }); }
  catch (e) { return res.json({ ok: false, error: 'bad-bet' }); }
  res.json(await store.placeBet(req.username, matchId, bet));
});

app.get('/api/bets', requireAuth, async (req, res) => {
  res.json(await store.listBets(req.username));
});

module.exports = app;
```

- [ ] **Step 5: Run to verify pass** — `npx jest server/tests/app.test.js`. Expected: PASS (4).

- [ ] **Step 6: Run the whole suite** — `npm test`. Expected: all green.

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json server/app.js server/tests/app.test.js
git commit -m "feat: express REST API (auth, accounts, events, bets)"
```

---

### Task 4: Web UI (`public/index.html`)

**Files:**
- Create: `server/public/index.html`

**Interfaces:**
- Consumes: the `/api/*` endpoints from Task 3. Self-contained (HTML + CSS + vanilla JS).

- [ ] **Step 1: Create `server/public/index.html`**

```html
<!doctype html>
<html lang="fr">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>lolbet</title>
<style>
  :root { --bg:#0e1015; --card:#1a1d27; --ink:#e7e9ee; --mut:#8a90a2; --acc:#f0b132; --win:#3ad17a; --lose:#e2545a; }
  * { box-sizing:border-box; } body { margin:0; font-family:system-ui,sans-serif; background:var(--bg); color:var(--ink); }
  header { padding:14px 18px; background:var(--card); display:flex; justify-content:space-between; align-items:center; }
  header b { color:var(--acc); } .bal { color:var(--acc); font-weight:700; }
  main { max-width:760px; margin:0 auto; padding:18px; }
  .card { background:var(--card); border-radius:12px; padding:16px; margin-bottom:16px; }
  h2 { margin:0 0 10px; font-size:15px; color:var(--mut); text-transform:uppercase; letter-spacing:.5px; }
  input,button { font:inherit; padding:9px 12px; border-radius:8px; border:1px solid #2b2f3c; background:#11141c; color:var(--ink); }
  button { background:var(--acc); color:#1a1205; border:0; font-weight:700; cursor:pointer; }
  button.ghost { background:#222633; color:var(--ink); }
  .row { display:flex; gap:8px; flex-wrap:wrap; align-items:center; }
  .ev { border:1px solid #262a36; border-radius:10px; padding:12px; margin-bottom:10px; }
  .ev h3 { margin:0 0 8px; font-size:15px; }
  .mkt { display:flex; justify-content:space-between; align-items:center; padding:5px 0; border-top:1px solid #20242f; }
  .mkt .sides { display:flex; gap:6px; }
  .odd { background:#222633; border:0; color:var(--ink); padding:6px 9px; border-radius:7px; cursor:pointer; font-weight:600; }
  .odd b { color:var(--acc); }
  .muted { color:var(--mut); font-size:13px; }
  .bet { display:flex; justify-content:space-between; padding:6px 0; border-top:1px solid #20242f; font-size:14px; }
  .won { color:var(--win); } .lost { color:var(--lose); }
  .hide { display:none; }
</style>
</head>
<body>
<header><span><b>lolbet</b> 🎰</span><span id="who" class="muted"></span></header>
<main>
  <div class="card" id="authCard">
    <h2>Connexion</h2>
    <div class="row">
      <input id="u" placeholder="pseudo" /><input id="p" type="password" placeholder="mot de passe" />
      <button onclick="login()">Login</button><button class="ghost" onclick="register()">Créer</button>
    </div>
    <div id="authMsg" class="muted"></div>
  </div>

  <div id="app" class="hide">
    <div class="card">
      <h2>Mes comptes LoL</h2>
      <div id="accs" class="muted"></div>
      <div class="row" style="margin-top:8px">
        <input id="riot" placeholder="Pseudo#TAG" /><button onclick="link()">Lier</button>
      </div>
      <div id="linkMsg" class="muted"></div>
    </div>

    <div class="card">
      <h2>Paris en cours</h2>
      <div id="events" class="muted">Aucun event ouvert. Lance une ranked (avec le poller actif) 👀</div>
    </div>

    <div class="card">
      <h2>Mes paris</h2>
      <div id="bets" class="muted">—</div>
    </div>
    <button class="ghost" onclick="logout()">Déconnexion</button>
  </div>
</main>
<script>
const $ = (id) => document.getElementById(id);
const api = (p, opt) => fetch('/api' + p, Object.assign({ headers: { 'Content-Type': 'application/json' } }, opt)).then((r) => r.json());

async function refresh() {
  const me = await api('/me').catch(() => null);
  if (!me || !me.username) { $('authCard').classList.remove('hide'); $('app').classList.add('hide'); $('who').textContent = ''; return; }
  $('authCard').classList.add('hide'); $('app').classList.remove('hide');
  $('who').innerHTML = me.username + ' · <span class="bal">' + me.balance + ' Clout</span>';
  renderAccounts(); renderEvents(); renderBets();
}
async function register() {
  const r = await api('/register', { method:'POST', body: JSON.stringify({ username:$('u').value, password:$('p').value }) });
  $('authMsg').textContent = r.ok ? 'Compte créé, connecte-toi.' : 'Échec: ' + r.error;
}
async function login() {
  const r = await api('/login', { method:'POST', body: JSON.stringify({ username:$('u').value, password:$('p').value }) });
  if (r.ok) refresh(); else $('authMsg').textContent = 'Échec: ' + r.error;
}
async function logout() { await api('/logout', { method:'POST' }); refresh(); }
async function link() {
  const r = await api('/link', { method:'POST', body: JSON.stringify({ riotId:$('riot').value }) });
  $('linkMsg').textContent = r.ok ? 'Lié ✅' : 'Échec: ' + r.error; renderAccounts();
}
async function renderAccounts() {
  const a = await api('/accounts');
  $('accs').textContent = a.length ? a.map((x) => x.riot_id).join(' · ') : 'aucun compte lié';
}
async function renderEvents() {
  const evs = await api('/events');
  if (!evs.length) { $('events').textContent = 'Aucun event ouvert.'; return; }
  $('events').innerHTML = evs.map((ev) => {
    const mkts = (ev.board || []).map((m) =>
      '<div class="mkt"><span>' + m.title + '</span><span class="sides">' +
      '<button class="odd" onclick="bet(\'' + ev.match_id + '\',\'' + m.id + '\',\'yes\')">' + m.yes.label + ' <b>' + m.yes.odds.toFixed(2) + '</b></button>' +
      '<button class="odd" onclick="bet(\'' + ev.match_id + '\',\'' + m.id + '\',\'no\')">' + m.no.label + ' <b>' + m.no.odds.toFixed(2) + '</b></button>' +
      '</span></div>').join('');
    return '<div class="ev"><h3>🎮 ' + ev.champion + ' <span class="muted">(' + ev.riot_id + ')</span></h3>' + mkts + '</div>';
  }).join('');
}
async function bet(matchId, marketId, side) {
  const stake = Number(prompt('Mise (Clout) ?', '50'));
  if (!stake) return;
  const r = await api('/bet', { method:'POST', body: JSON.stringify({ matchId, marketId, side, stake }) });
  if (r.ok) refresh(); else alert('Pari refusé: ' + r.error);
}
async function renderBets() {
  const bs = await api('/bets');
  if (!bs.length) { $('bets').textContent = 'aucun pari'; return; }
  $('bets').innerHTML = bs.map((b) => {
    const cls = b.status === 'won' ? 'won' : b.status === 'lost' ? 'lost' : 'muted';
    const res = b.status === 'open' ? 'en cours' : b.status === 'won' ? '+' + b.payout : 'perdu';
    return '<div class="bet"><span>' + b.match_id + ' · ' + b.stake + '</span><span class="' + cls + '">' + res + '</span></div>';
  }).join('');
}
refresh();
</script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add server/public/index.html
git commit -m "feat: minimal web UI (login, link, events, bet, history)"
```

---

### Task 5: `serve` CLI + manual smoke

**Files:**
- Create: `server/index.js` (HTTP entrypoint — listens)
- Modify: `package.json` (add `serve` script)

**Interfaces:**
- Consumes: `app` from `./app`; `db.init`.
- Produces: `npm run serve` boots the API + UI on `PORT` (default 3000).

- [ ] **Step 1: Create `server/index.js`**

```js
// server/index.js — HTTP entrypoint.
const app = require('./app');
const db = require('./db');

const PORT = process.env.PORT || 3000;

db.init().then((ready) => {
  if (!ready) console.warn('[serve] DATABASE_URL absent — l\'app tourne mais rien n\'est persisté.');
  app.listen(PORT, () => console.log(`🎰 lolbet sur http://localhost:${PORT}`));
});
```

- [ ] **Step 2: Add the `serve` script to package.json** `scripts`:

```json
    "serve": "node server/index.js",
```

- [ ] **Step 3: Smoke test (no DB)** — start the server, hit the events endpoint, stop it:

Run: `PORT=3010 node server/index.js & sleep 1 && curl -s localhost:3010/api/events && curl -s -o /dev/null -w "%{http_code}" localhost:3010/ ; kill %1`
Expected: `[]` from `/api/events`, and `200` for `/` (serves index.html).

- [ ] **Step 4: Run the full suite** — `npm test`. Expected: all green.

- [ ] **Step 5: Commit**

```bash
git add server/index.js package.json
git commit -m "feat: serve command (HTTP entrypoint for API + UI)"
```

---

## Self-Review

**Spec coverage:** register/login (Task 1+3), link Riot trust-based (Task 3 `/api/link` via getPuuid), see open events with odds (Task 3 `/api/events` + Task 4 UI), place bet from frozen board odds (Task 3 `/api/bet`), balance + history (Task 3 `/api/me` + `/api/bets`, Task 4 UI). ✓ Makes the engine usable by humans (MVP). ✓

**Placeholder scan:** complete code in every step (session, store fn, full app.js, full index.html, entrypoint). No TBD. ✓

**Type consistency:** `session.sign/read` used by app cookie middleware; `makeSingle({player, board, marketId, side, stake})` matches Slice-1 signature, fed `ev.board` (the JSONB priced board from events). `store.placeBet(username, matchId, bet)` / `store.listBets(username)` / `store.getBalance` match existing/Task-2 signatures. `events.listOpen`/`getEvent` shapes (`match_id`, `champion`, `riot_id`, `board`) match the UI's usage. ✓
