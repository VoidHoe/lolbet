{
  "name": "lolbet-proto",
  "version": "0.0.1",
  "private": true,
  "description": "De-risk prototype: auto-settle bets from real LoL games via the Riot API.",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "backtest": "node src/index.js backtest",
    "watch": "node src/index.js watch"
  }
}
